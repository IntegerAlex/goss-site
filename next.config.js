/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "my-gram-images.s3.ap-south-1.amazonaws.com"
        ]
    }
}

module.exports = nextConfig
